import styles from "./Footer.module.scss";

const Footer = () => {
	return (
		<div className={styles.footer}>
			<div className={styles.footer__texto}>2022 - Desenvolvido por Alura.</div>
		</div>
	);
};

export default Footer;
